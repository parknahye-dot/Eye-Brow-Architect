# production environment properties
spring.datasource.url=jdbc:mysql://terraform-20260309023616760500000001.cf64ggy2eo8o.ap-northeast-2.rds.amazonaws.com:3306/eyebrow_architect?serverTimezone=UTC&useUniCode=yes&characterEncoding=UTF-8
spring.datasource.username=nahye_admin
spring.datasource.password=nahye1234
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# AWS S3
spring.cloud.aws.s3.bucket=brow-architect-storage-nahye
spring.cloud.aws.region.static=ap-northeast-2

# 6. 파일 업로드 용량 제한
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# AWS Lex V2 (실제 연동용)
amazon.lex.botId=93G9BEFBTR
amazon.lex.botAliasId=TSTALIASID
amazon.lex.localeId=ko_KR

# Logging
logging.level.com.eyebrowarchitect=DEBUG
logging.file.name=server_debug.log

# AI Service (Python FastAPI)
ai.service.url=${AI_SERVICE_URL:http://ai-service:8000/analyze}

# Storage Mode (Force S3 in production)
app.storage.local=false

# AWS Credentials mapping (Ensures S3Service picks up injected env vars)
spring.cloud.aws.credentials.access-key=${AWS_ACCESS_KEY_ID:NONE}
spring.cloud.aws.credentials.secret-key=${AWS_SECRET_ACCESS_KEY:NONE}
